@model SampleStore.WebUI.Models.PagingInfo

<table>
    <tr>
    <td class="pageLinksTotal">@Model.TotalPages</td>

    @{
        // Число отображенных страниц
        int pagesSeen = 3;
        // Отобразим 
        for (int pageNumber = 0; pageNumber < Model.TotalPages;pageNumber++ )
        {
            
        }
    }
    <td>@Model.SelectedPage</td>

    </tr>
</table>

